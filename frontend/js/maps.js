function getCurrentLocation(latId='latitude',lngId='longitude'){if(!navigator.geolocation)return alert('Geolocation is not supported');navigator.geolocation.getCurrentPosition(p=>{document.getElementById(latId).value=p.coords.latitude.toFixed(6);document.getElementById(lngId).value=p.coords.longitude.toFixed(6);const map=document.getElementById('map');if(map)map.innerHTML=`Location captured: ${p.coords.latitude.toFixed(6)}, ${p.coords.longitude.toFixed(6)}`},()=>alert('Location permission was denied.'))}