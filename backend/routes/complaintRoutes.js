const r=require('express').Router();const c=require('../controllers/complaintController');const auth=require('../middleware/auth');const roles=require('../middleware/role');const multer=require('multer');const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:5*1024*1024},fileFilter:(req,file,cb)=>cb(null,/^image\/(jpeg|png|webp)$/.test(file.mimetype))});r.post('/',auth,roles('CITIZEN'),upload.single('image'),c.create);r.get('/',auth,c.list);r.get('/my',auth,roles('CITIZEN'),c.list);r.get('/:id',auth,c.getOne);r.put('/:id',auth,roles('CITIZEN'),c.update);r.delete('/:id',auth,roles('CITIZEN'),c.remove);module.exports=r;